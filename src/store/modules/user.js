const state = {
  user: {
    name: 'RexSun'
  }
}

const getters = {
  user: (state) => {
    return state.user
  }
}

const mutations = {
}

const actions = {
}

export default {
  state,
  mutations,
  getters,
  actions
}
